package org.example;

import static org.junit.jupiter.api.Assertions.*;
import org.junit.jupiter.api.Test;

public class ExampleTestTiposAsserts {

    //Probar metodo sumar que esta en la clase Example.
    @Test
    public void testSumar(){

        /*Crear una instancia del objeto o clase a testear.
        Example example = new Example();

        //Llamar al metodo el cual vamos a testear.
        int result = example.suma(10,4);

        //Anotacion de Junit para validar el test, colocando primero el valor esperado y despues el valor actual de la respueta del metodo.
        //Assertions.assertEquals(14, result); // Evaluar valor esperado con un valor actual.
        //Assertions.assertTrue(result > 20); //Valida que sea verdadero.
        //Assertions.assertFalse(result > 20); //Valida que sea false.
        //Assertions.assertNotNull(result); //Valida que el objeto respuesta no sea nulo.
        //assertInstanceOf(String.class, result); // Valida el tipo de objeto.
        //Assertions.assertThrows(); //Valida las excepciones.
        */
    }
}
